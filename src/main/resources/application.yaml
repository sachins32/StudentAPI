spring:
  datasource:
    url: jdbc:mysql://localhost:3306/school
    user: sachin
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true